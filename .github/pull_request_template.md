<!--
Title: name this PR "Release: YYYY.MM.NN" to match the release branch.

Style (see PRs #39–#41 for examples):
  - Group changes under **bold category** headers (only the ones that apply):
    Scripts · Worktrees · Install · Setup · Prompt · Shell cleanup ·
    Brew · Ghostty · Runtime management — or add a new one as needed.
  - Each entry: `name` — what it does, AND the *why* when it isn't obvious.
    Reference real paths (`install/foo`, `.zshrc`, `settings/bar/baz.toml`).
  - Backtick command/file/tool names. Call out deliberate, surprising
    choices explicitly (e.g. "uses npm -g, not pnpm, because …").
  - Delete every unused section and this comment before opening.
-->

## Release: YYYY.MM.NN

**Scripts**
- `name` — what it does; why, if non-obvious

**Worktrees**
-

**Install**
-

**Setup**
-

**Prompt**
-

**Shell cleanup**
-

**Brew**
- `Brewfile.<set>` — formula/cask added or removed, and why
