# LLM related utilities

# ~/.claude/settings.json: hook added
# filters:  ~/Library/Application Support/rtk/filters.toml (template, edit to add user-global filters)
# rtk: installed from source at ~/Git/indexzero/rtk (cargo install --path .)
brew "rtk-ai/tap/icm"
cask "codex"
# Jq-like command-line tool for markdown processing
brew "mq"
# Persistent memory for your coding agents
brew "vshulcz/tap/deja-vu"
